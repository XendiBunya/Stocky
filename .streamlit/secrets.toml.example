# Example secrets file for Streamlit Cloud
# Copy this to secrets.toml and add your actual API keys
# DO NOT commit secrets.toml to version control

# NewsAPI key (optional - get from https://newsapi.org/)
# If not provided, the app will use Yahoo Finance as fallback
# newsapi_key = "your_api_key_here"
